version: '3.4'

services:
  authservice:
    image: auth-service
    build:
      context: ./
      dockerfile: ./Dockerfile
    container_name: auth-service
    restart: 'always'    
    ports:
      - 5089:80
    